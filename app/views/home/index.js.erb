$('#posts').append("<%= j(render :partial => @posts)%>");

$('#load_more_link').replaceWith("<%= escape_javascript(link_to_next_page(@posts, 'Carregar mais posts', :remote => true, :id=>'load_more_link'))%>");
